
// Firebase dihapus dari aplikasi untuk beralih ke penyimpanan lokal (LocalStorage).
// Fitur ini membantu menghindari error "Service firestore is not available".
export const db = null as any;
export const storage = null as any;
